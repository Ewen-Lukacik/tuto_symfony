{% extends  'base.html.twig' %}

{% block title %}

    Toutes les recettes

{% endblock title %}


{% block body %}


voici les recettes disponibles

<br><br>
 
<a class="btn btn-outline-primary btn-sm" href="{{ path('recipe.create') }}">
    Ajouter une recette 
</a>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Actions</th>
        </tr>
    </thead>
    {% for recipe in recipes %}
    <tr>
        <td>
            <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">
                {{ recipe.title }}
            </a>
        </td>
        <td>
            <div class="d-flex gap-1">
                <a class="btn btn-primary btn-sm" href="{{ path('recipe.edit', {id: recipe.id}) }}">
                    Edit
                </a>
                <form action="{{ path('recipe.delete', {id: recipe.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
                    <button class="btn btn-danger btn-sm" type="submit">DELETE</button>
                </form>
            </div>
        </td>
    </tr>
   
    {% endfor %}
</table>
{% endblock body %}